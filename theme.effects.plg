<?xml version='1.0' standalone='yes'?>
<!DOCTYPE PLUGIN [
<!ENTITY name      "theme.effects">
<!ENTITY author    "deltrivx">
<!ENTITY version   "v2.8.7">
	<!ENTITY launch    "Settings/ThemeEffects">
	<!ENTITY plugdir   "/usr/local/emhttp/plugins/&name;">
	<!ENTITY flash     "/boot/config/plugins/&name;">
	<!ENTITY github    "deltrivx/ThemeEffects">
	<!ENTITY pluginURL "https://raw.githubusercontent.com/&github;/main/theme.effects.plg">
	<!ENTITY ver       "v2.8.7">
	<!ENTITY installSH "https://github.com/&github;/releases/download/&ver;/install.sh">
	]>
	<PLUGIN name="&name;" author="&author;" version="&version;" launch="&launch;" pluginURL="&pluginURL;" min="6.12.0" icon="magic" support="https://github.com/&github;">
	
	<!-- CHANGES in CDATA: bare & in notes must not break simplexml_load_file -->
	<CHANGES>
	<![CDATA[
		## Theme Effects
			### v2.8.7
			- 全局左上角版本小窗复用配置页的异步更新任务与实时进度
			- 更新弹窗显示百分比、阶段、滚动日志、重试状态和完成/失败操作
			### v2.8.6
			- 修复左上角 ThemeEffects 版本小窗仍显示历史版本的问题
			- 最新版时点击首行查看当前版本更新日志；有新版时显示远程版本并进入 OTA / 全量更新操作
			- 移除小窗内重复的更新日志入口，不包含视觉样式、尺寸、配色或布局调整
			### v2.8.5
			- 放大当前版本更新日志和版本管理弹窗，使其与系统弹窗使用相同的视口级尺寸
			- 取消更新日志和版本列表过小的固定高度，改为弹性布局和可滚动内容区
			- 修复版本管理弹窗打开时未启用 flex 布局的问题
			### v2.8.4
			- 修复 Dashboard 卡片标题、图标、两行摘要和切换控件对齐
			- 修复 Docker/VM 内容区底部重复空余，并保持原生项目间距
			- 修复共享卡片 SMB/NFS 控件前方空余与视觉不统一
			- 对齐 ThemeEffects 版本小窗与系统版本菜单，新增更新状态和 Issues 反馈入口
			- ThemeMusic 保持独立，不包含音乐运行代码
			### v2.8.2
			- 新增独立滚动条全局颜色和页面覆盖设置
			- 修复颜色块选择后点击应用颜色被还原
			- 分离字体与滚动条配置保存，避免分段互相覆盖
			### v2.8.1
			- 统一 PC 与移动端 Dashboard 卡片标题、摘要和控件排列，修复遮挡、重叠与异常空白
			- 修复 Dashboard 内容区域纵向对齐，保持原生表格结构和横向布局
			- 恢复 Docker/VM 正式版原生浮动排列、固定内容宽度、间距和停止项筛选
			- 修复移动端 Docker/VM 两列排列，以及容器图标、名称和状态行显示
			- 优化接口、用户、共享、系统卡片标题控件和 Dashboard 自定义下拉菜单
			- ThemeMusic 继续由独立插件维护，不包含音乐运行代码

		### v2.7.2

	- 修复历史 v2.5.1 遗留 latest 频道导致 v2.7.0 无法检测新版本
	- 检查更新改为优先使用顶层 latest_version，不再被历史频道标记劫持
	- 发布校验强制 latest 频道唯一且必须与 default、latest_version 和索引首项一致
	### v2.7.1
	- 代码许可改为 PolyForm Noncommercial 1.0.0，明确禁止未经授权的商业使用
	- 更新日志版本列表自动换行并禁止横向滚动，窄屏改为单列布局
	- 首次安装默认关闭，须在设置页手动开启；升级继续保留用户原有开关
	### v2.7.0
	- 清除 2.0 以前的版本历史与旧插件专属失效文件，保留用户 CSS 和资源
	- PLG、终端 OTA、终端全量统一为 Release 归档双重 SHA256 安装链路
	- 三种安装方式自动清除 Theme Effects 自身过期 Loader、音乐组件和缓存
	- Release 标准化为六项产物，仓库中文文档、协议与自动验证全面完善
	###2026.07.31b
	- v2.6.4: boot restore-from-flash before OTA (official package; survive reboot offline/timeout)
	###2026.07.31a
	- boot: restore runtime from flash before OTA (survive reboot when GitHub times out)
	###2026.07.30y
	- v2.6.2: themed cursor on all interactions (hover/click/menus no longer fall back to system)
	###2026.07.30x
	- v2.6.1: purge legacy built-in music residue on install/upgrade (theme.music untouched)
	###2026.07.30w
	- v2.6.0 stable: remove all music components; fix custom cursor flash on menu page switch
###2026.07.30v
- v2.6.0-Beta32: CA title clears icon; desc above buttons; mobile FLAC error hard-reset + cross-page resume
###2026.07.30u
- v2.6.0-Beta31: 1.8.x-style CA card layout (desc inside card); mobile music sync-gesture play
###2026.07.30t
- v2.6.0-Beta30: restore stock CA card rhythm; unclip corner badges; mobile music gesture resume
###2026.07.30s
- v2.6.0-Beta29: fix Apps card overlap for modern CA (.cardDescription + collapse 18.5rem bands)
###2026.07.30r
- v2.6.0-Beta28: apps card desc/button overlap fix; music no-cover keeps default note art
###2026.07.30q
- v2.6.0-Beta27: real cover resolve — parent folder art, FLAC picture stream, clean artist/album search
###2026.07.30p
- v2.6.0-Beta26: fix album cover flash-then-gone (keep painted cover, deferred blob revoke)
###2026.07.30o
- v2.6.0-Beta25: OTA index TLS retry + multi-mirror fallback
###2026.07.30n
- v2.6.0-Beta24: fix card/chip click handlers blocked by capture stopPropagation
###2026.07.30m
- v2.6.0-Beta23: in-page chip overlay only; no popup/new tab; clicks control only
###2026.07.30l
- v2.6.0-Beta22: chip-styled mini host window, card/chip sync, no cross-page flash
###2026.07.30k
- v2.6.0-Beta21: play button follows real pause state after navigation
###2026.07.30j
- v2.6.0-Beta20: mobile list/lyrics toggle no width jump
###2026.07.30i
- v2.6.0-Beta19: mobile fixed width, resume seek, no track flash on enter
###2026.07.30h
- v2.6.0-Beta18: cover display, mobile stack, dash pos memory, resume progress
###2026.07.30g
- v2.6.0-Beta17: track/lyrics long text horizontal marquee
###2026.07.30f
- v2.6.0-Beta16: music card album cover display + auto download fix
###2026.07.30e
- v2.6.0-Beta15: fileTree bg=title bar, progress no stage next to %, cursor file width align
###2026.07.30d
- v2.6.0-Beta14: fixed music side width, dash card position memory, album cover auto-download
###2026.07.30c
- v2.6.0-Beta13: cursor dual upload, drop music host, select=title bg, single progress %
###2026.07.30b
- v2.6.0-Beta12 reissue: keep chip; music host window for cross-page audio; cursor file width; no play/pause flicker
###2026.07.29w
- v2.6.0-Beta11: transform chip drag, muted unlock resume, netease lyric fallback, wallpaper-style cursor rows
###2026.07.29v
- v2.6.0-Beta10: auto-resume retries, chip drag fix, lyric proxy/cache, cursor hideRow
###2026.07.29u
- v2.6.0-Beta9: layout spacing, draggable chip resume+lyric, auto LRC download, custom style UI, anti multi-play
###2026.07.29t
- v2.6.0-Beta8: stronger sitewide resume; show custom cursor fields only when type=upload
###2026.07.29s
- v2.6.0-Beta7: local LRC lyrics sync (sidecar .lrc, scroll highlight)
###2026.07.29r
- v2.6.0-Beta6: cross-page resume; dual-pane music card; sitewide cursor + custom upload
###2026.07.29q
- v2.6.0-Beta5: music card dash-only UI; sitewide play without card; 14px glass tile
###2026.07.29p
- v2.6.0-Beta4: neon3d/holo/cyber/crystal cursors; music tile polish + lyrics slot; dash/site play
###2026.07.29o
- v2.6.0-Beta3: path field width measured from neighboring select (pixel align)
###2026.07.29n
- v2.6.0-Beta3: dash music tile, MOUSE_CURSOR system pointer styles, path/fileTree theme align
###2026.07.29m
- v2.6.0-Beta2: select chevrons + Unraid fileTree path/dir pickers
###2026.07.29k
- v2.6.0-Beta available via Check Beta: music V1 dashboard card + local media
- Plugins default install is stable v2.6.0
###2026.07.29j
- Bump plg version so Plugins reinstall/update is not skipped as same version
###2026.07.29i
- CDATA CHANGES + version bump so raw CDN / Plugins install always parse
###2026.07.29h
- Fix Plugins-page install hang: stream install progress to plugin UI (stdout + line-buffered)
###2026.07.29g
- v2.5.0: mouse FX stable (glow/ring/trail/spark), install %, check switch
###2026.07.29b
- v2.4.2: restore 1.8.x Docker/VM float layout & spacing
###2026.07.29a
- v2.4.1: fourth stable — docker/vm bottom gap, beta check floor, upload layout
###2026.07.29
- v2.4.0: third stable — dash close-tile/icons, perf profile rewrites effects, drop GPU_ACCEL
###2026.07.28
- v2.3.0: second stable — upload hints, Unraid fileTree, font preview, perf, first-run optimize
- v2.2.0: first stable 2.x — blur perf, blur levels, dual upload, SERVICE toggle
- v2.1.2-beta: font phase3 — custom family name + local woff2/ttf upload
- v2.1.1-beta / v2.1.0-beta: fonts, colors, title size
- v2.0.0-beta: standalone plugin theme.effects
]]>
</CHANGES>

<!-- Prepare dirs -->
<FILE Run="/bin/bash">
<INLINE>
mkdir -p &flash;/assets &plugdir;/assets
echo "Theme Effects: preparing &name;…"
exit 0
</INLINE>
</FILE>

<!-- README shown in plugin manager -->
<FILE Name="&plugdir;/README.md">
<INLINE>
<![CDATA[
# 主题特效（Theme Effects）

适用于 Unraid 6.12+ 的独立 WebGUI 视觉增强插件。

- 全局主题、壁纸、模糊与响应式布局
- 粒子、鼠标动效和多套全站指针
- 内置或自定义 GIF 吉祥物
- 字体、颜色、本地字体与应用页增强
- 双重 SHA256、差异 OTA、全量修复与 flash 离线恢复

安装后前往：**设置 → 用户偏好 → 主题特效**

项目主页、中文更新日志和支持信息：
https://github.com/deltrivx/ThemeEffects
]]>
</INLINE>
</FILE>

<!-- Boot/install: restore runtime from flash first (offline-safe), then OTA if network works -->
<FILE Run="/bin/bash">
<INLINE>
set +e
export PATH="/usr/local/sbin:/usr/sbin:/sbin:/usr/local/bin:/usr/bin:/bin"
export UCWC_PLUGIN_INSTALL=1
export UCWC_INSTALL_MODE="${UCWC_INSTALL_MODE:-ota}"
FLASH="&flash;"
PLUG="&plugdir;"
mkdir -p "$FLASH/assets" "$PLUG/assets"

echo "===================================================="
echo " Theme Effects plugin install"
echo " package: &ver;  mode: $UCWC_INSTALL_MODE"
echo "===================================================="

restore_from_flash() {
  [ -f "$FLASH/ThemeEffects.page" ] || return 1
  [ -f "$FLASH/ThemeEffects_Loader.page" ] || return 1
  echo "Theme Effects: restoring runtime from flash…"
  for f in ThemeEffects.page ThemeEffects_Loader.page apps-enhancement.js style.css style-black.css README.md \
           ucwc-theme-fx-save.php ucwc-update.php ucwc-auth-request.conf ucwc-upload.ini; do
    [ -f "$FLASH/$f" ] &amp;&amp; install -m 0644 "$FLASH/$f" "$PLUG/$f"
  done
  for f in apps-enhancement.js ucwc-mouse-fx.js ucwc-particles.js ucwc-theme-fx.css ucwc-theme-fx.js; do
    [ -f "$FLASH/assets/$f" ] &amp;&amp; install -m 0644 "$FLASH/assets/$f" "$PLUG/assets/$f"
  done
  if [ -d "$FLASH/assets/fonts" ]; then
    mkdir -p "$PLUG/assets/fonts"
    cp -a "$FLASH/assets/fonts/." "$PLUG/assets/fonts/" 2>/dev/null || true
  fi
  for f in background.jpg background-1.jpg background-2.jpg background-custom.jpg background-dynamic.jpg hutao.gif mascot-custom.gif; do
    if [ -f "$FLASH/assets/$f" ] &amp;&amp; [ ! -e "$PLUG/assets/$f" ]; then
      ln -sf "$FLASH/assets/$f" "$PLUG/assets/$f" 2>/dev/null || cp -a "$FLASH/assets/$f" "$PLUG/assets/$f"
    fi
  done
  return 0
}

if restore_from_flash; then
  echo "Theme Effects: flash restore OK"
else
  echo "Theme Effects: no complete flash package yet; will need network install"
fi

if ! command -v curl >/dev/null 2&gt;&amp;1; then
  if [ -f "$PLUG/ThemeEffects.page" ]; then
    echo "Theme Effects: curl missing but flash runtime present — boot OK"
    exit 0
  fi
  echo "ERROR: curl required" &gt;&amp;2
  exit 1
fi

echo "[1/4] downloading install.sh…"
TMP=$(mktemp /tmp/theme.effects.install.XXXXXX)
ok=0
if curl -4 -fsSL --connect-timeout 8 --max-time 30 --retry 0 \
    -o "$TMP" "&installSH;?_ts=$(date +%s)"; then
  ok=1
else
  for m in \
    "https://ghfast.top/https://github.com/&github;/releases/download/&ver;/install.sh" \
    "https://raw.githubusercontent.com/&github;/main/scripts/install.sh" \
    "https://cdn.jsdelivr.net/gh/&github;@main/scripts/install.sh" \
    "https://fastly.jsdelivr.net/gh/&github;@main/scripts/install.sh" \
    "https://raw.gitmirror.com/&github;/main/scripts/install.sh" \
    "https://ghfast.top/https://raw.githubusercontent.com/&github;/main/scripts/install.sh"
  do
    if curl -4 -fsSL --connect-timeout 8 --max-time 30 --retry 0 -o "$TMP" "$m"; then ok=1; break; fi
  done
fi

if [ "$ok" = "1" ] &amp;&amp; [ -s "$TMP" ]; then
  chmod +x "$TMP"
  echo "[1/4] install.sh ready ($(wc -c &lt; "$TMP" | tr -d ' ') bytes)"
  echo "[2/4] running install &ver;…"
  if command -v stdbuf >/dev/null 2&gt;&amp;1; then
    stdbuf -oL -eL sh "$TMP" install "&ver;" 2&gt;&amp;1
  else
    sh "$TMP" install "&ver;" 2&gt;&amp;1
  fi
  rc=$?
  rm -f "$TMP"
  if [ "$rc" -eq 0 ]; then
    echo "Theme Effects: OTA install OK (&ver;)"
  else
    echo "Theme Effects: OTA returned $rc — falling back to flash restore"
    restore_from_flash
  fi
else
  echo "Theme Effects: cannot download install.sh (network)"
  rm -f "$TMP"
  restore_from_flash
fi

echo "[3/4] verifying runtime files…"
if [ -f "$PLUG/ThemeEffects.page" ]; then
  echo "Theme Effects: runtime present — boot OK"
  echo "----------------------------------------------------"
  echo " theme.effects &ver; ready."
  echo " Open Settings → User Preferences → 主题特效"
  echo "----------------------------------------------------"
  exit 0
fi
echo "Theme Effects: ERROR — no runtime files (flash empty and network failed)" &gt;&amp;2
exit 1
</INLINE>
</FILE>

<!-- Remove -->
<FILE Run="/bin/bash" Method="remove">
<INLINE>
set +e
export PATH="/usr/local/sbin:/usr/sbin:/sbin:/usr/local/bin:/usr/bin:/bin"
export UCWC_PLUGIN_INSTALL=1
echo "Theme Effects: uninstalling…"
TMP=$(mktemp /tmp/theme.effects.uninstall.XXXXXX)
if curl -4 -fsSL --connect-timeout 8 --max-time 30 -o "$TMP" "&installSH;?_ts=$(date +%s)" 2>/dev/null; then
  chmod +x "$TMP"
  if command -v stdbuf >/dev/null 2>&amp;1; then
    stdbuf -oL -eL sh "$TMP" uninstall 2&gt;&amp;1 || true
  else
    sh "$TMP" uninstall 2&gt;&amp;1 || true
  fi
fi
rm -f "$TMP"
rm -rf &plugdir;
# Keep &flash; user assets by default; uncomment to wipe:
# rm -rf &flash;
echo "theme.effects removed (flash config retained under &flash;)."
exit 0
</INLINE>
</FILE>

</PLUGIN>
